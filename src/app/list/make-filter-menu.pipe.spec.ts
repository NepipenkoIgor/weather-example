import { MakeFilterMenuPipe } from './make-filter-menu.pipe';

describe('MakeFilterMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new MakeFilterMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
